services:
  face-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aipos-face-service
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
    mem_limit: 768m
    cpus: 1.0
